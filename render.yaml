services:
  - type: web
    name: heartbridge-unified
    env: python
    buildCommand: ""
    startCommand: uvicorn app:app --host 0.0.0.0 --port 10000
    plan: free
    region: oregon
    autoDeploy: true