services:
  - type: web
    name: heartbridge-unified
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.app:app -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:10000
    plan: free
    region: oregon
    autoDeploy: true
