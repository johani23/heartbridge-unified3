services:
  - type: web
    name: heartbridge-unified
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.app:app --host 0.0.0.0 --port 10000
    plan: free
    region: oregon
    autoDeploy: true

